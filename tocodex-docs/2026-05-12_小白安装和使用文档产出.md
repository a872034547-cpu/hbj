# 小白安装和使用文档产出

## 摘要

新增面向零基础用户的 Windows 安装和使用说明文档，覆盖从环境安装、项目下载、虚拟环境创建、依赖安装、模型配置、网页启动，到完整写书流程、导出 Word/PDF/PPTX、常见问题处理和常用命令汇总。

## 新增文件

- `tocodex-docs/2026-05-12_小白安装和使用文档.md`

## 主要内容

- Python、Git、VS Code、Word/WPS、Pandoc 等前置环境安装命令。
- 项目下载、进入目录、创建 `.venv`、激活虚拟环境命令。
- 使用 `requirements.txt` 和国内源安装依赖的命令。
- Web 端启动命令：`python -m streamlit run src\libriscribe\web\app.py`。
- 模型 API Key 配置说明。
- 从创建项目到上传资料、生成大纲、生成正文、生成前言结语和导出的完整流程。
- 常见问题排查命令。
- 一键启动 `start_web.bat` 示例。
