# Linux 新服务器小白安装文档

## 摘要

为项目新增了一份 Linux 服务器部署文档，面向零基础用户，覆盖从登录服务器、安装系统依赖、下载项目、创建虚拟环境、安装 Python 依赖、配置 API Key、启动 Streamlit、开放端口、systemd 后台运行、Nginx 域名反代、HTTPS、备份、更新和常见问题排查的完整流程。

## 变更文件

- `tocodex-docs/2026-05-12_Linux新服务器小白安装文档.md`

## 关键内容

- Ubuntu / Debian 服务器基础依赖安装。
- `/opt/libriscribe` 推荐部署目录。
- Python `.venv` 虚拟环境安装流程。
- `requirements.txt` 和 `pip install -e .` 安装方式。
- `streamlit` 手动启动命令。
- 8501 端口、防火墙和云安全组说明。
- `systemd` 后台服务和开机自启配置。
- Nginx 反向代理与 HTTPS 配置。
- 项目更新、数据备份、安全建议和小白排错清单。
