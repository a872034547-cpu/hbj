# 参考文献 AI 主流程恢复修复

## 摘要
- 修复参考文献生成仍被真实 citation 记录硬性拦截的问题。
- 参考文献生成现在以 AI 提示词生成为主流程，不再要求当前项目必须先有真实 citation 记录。
- 当 AI 返回内容但格式不符合系统识别规则时，会先追加一次 AI 格式整理；如果仍无法识别，也会写入 AI 返回内容供页面继续编辑，避免生成结果丢失。
- 仅在模型空响应或超时时，继续使用项目已有 citation、OpenAlex 或编辑框已有有效文献作为兜底。
- 已通过 `python -m py_compile src/libriscribe/web/app.py` 语法验证，并确认 `manuscript_references` 全局提示词可正常读取。

## 变更文件
- `libriscribe/src/libriscribe/web/app.py`

## 服务器同步提醒
- 需要把本次修改提交并推送到 GitHub。
- 服务器需要拉取最新代码并重启 Streamlit 服务，否则仍会运行旧代码。
