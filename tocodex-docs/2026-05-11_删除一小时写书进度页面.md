# 删除一小时写书进度页面

## 日期
2026-05-11

## 摘要
删除所有"一小时写书进度"页面内容：
1. `render_pipeline_page()` — 整个函数体替换为自动重定向到首页（Workspace），不再渲染任何 Pipeline 内容
2. `render_workspace_page()` — 移除"一小时写书进度"区块（pipeline 阶段卡片、路径说明、一小时流程指标），简化为三列指标（资料/文献、大纲单元、正文完成度）

## 变更文件
- `libriscribe/src/libriscribe/web/app.py` — `render_pipeline_page()` 和 `render_workspace_page()` 函数

## 测试
- 全部 97 个测试通过
